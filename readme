Chay nhanh (tu root `umc`):

npm run kill:3000
npm run backend:restart

Hoac chay thu cong:

cd backend
npm run start:dev

cd frontend
npm run dev

git add .
git commit -m "tự động"
git push

AI chatbox setup (backend):

cd backend
npm run ai:seed-knowledge
