// src/pages/AdminDashboardPage.tsx
export default function AdminDashboardPage() {
    return (
        <div className="p-8">
            <h1 className="text-2xl font-bold text-[hsl(var(--foreground))]">Admin Dashboard</h1>
            <p className="text-[hsl(var(--muted-foreground))] mt-2">Đây là trang tổng quan quản trị viên. Nội dung sẽ được phát triển tiếp.</p>
        </div>
    );
}
